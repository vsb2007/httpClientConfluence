import bgroup.model.DataPage;
import org.junit.Assert;
import org.junit.Test;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;
import org.springframework.context.support.AbstractApplicationContext;

/**
 * Created by VSB on 10.08.2017.
 * httpClientConfluence
 */
public class DadaPageTest {

    @Test
    public void getDataPageTest(){
       /*
        AbstractApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);
        DataPageService dataPageService = (DataPageService) context.getBean("dataPageService");
        DataPage dataPage = dataPageService.getDataPageList().get(0);

        Assert.assertEquals("id1",dataPage.getPageId());
        Assert.assertEquals("id1",dataPage.getPageId());
*/
    }
}
